[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pd-estim-A"
version = "0.1.0"
description = "Structural credit risk models: Merton and NIG extension"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]